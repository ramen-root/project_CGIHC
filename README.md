# project_CGIHC
Este proyecto es una implementación de una galería de arte virtual con OpenGL.
Debido al peso, los modelos (que se almacenan en el directorio /Models) no se encuentran en el repositorio. Por los mismo motivos, el directorio *External Libraries* y *Release* se eliminaron.
